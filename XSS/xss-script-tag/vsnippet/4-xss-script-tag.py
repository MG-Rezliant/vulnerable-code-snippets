from flask import Flask, request, render_template
from ignore.design import design
title = Vsnippet #4 - Cross Site Scripting XSS script tag outbreak
app = design.DesignFlask__name__, __file__, title

##
# YesWeHack - Vulnerable code snippets
##

def renderHTMLstr:
    HTML = 
    h2 id=welcomeWelcome: /h2
    script
        name = %s
        out = document.getElementByIdwelcome
        out.innerHTML += name
    /script
     % str

    return HTML

@app.route